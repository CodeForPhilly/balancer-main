apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - gateway-listeners.yaml
  - httproute.yaml

configMapGenerator:
  - name: balancer-config
    envs:
      - balancer.env
